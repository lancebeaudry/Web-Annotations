{
  "name": "avalanche-markup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Avalanche Markup \u2014 click-to-comment visual feedback overlay for client sites",
  "scripts": {
    "build": "node build.mjs",
    "build:mock": "node build.mjs --mock",
    "dev": "node build.mjs --watch",
    "new-site": "node admin/new-site.mjs",
    "release": "node admin/release.mjs",
    "release:dry": "node admin/release.mjs --dry-run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
